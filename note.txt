“I built an end-to-end forecasting pipeline where weather data is
 ingested via API, persisted in PostgreSQL, processed through feature
  engineering, and used to train a time-series model with evaluation using RMSE. 
 pipeline is modular and can be orchestrated using Airflow.”

 “I encountered a pandas datetime parsing issue and resolved it by
  ensuring proper imports and datatype conversion before feature engineering.”

  ROOT PROBLEM

👉 Airflow 3.x ignores old user creation
👉 It ONLY reads users from environment variables at startup
I built a full ML forecasting system with uncertainty quantification using 
SARIMAX, exposing predictions via FastAPI and visualizing results with 
confidence intervals and RMSE in a dashboard.

I developed an end-to-end time series forecasting system using SARIMAX with 
exogenous variables, including uncertainty estimation via confidence intervals.
The system is deployed using FastAPI and visualized in an interactive dashboard 
with performance metrics.

“I built an end-to-end ML system with FastAPI, Docker, and AWS EC2. Initially, 
it used a simulation-based model, but I upgraded it to a real ML model using 
XGBoost with persisted inference. I separated training and prediction pipelines,
 added monitoring with Prometheus, and structured it like a production system
  with CI/CD readiness.”